#include <stdio.h>

#include <time.h>

#include <stdlib.h>

/**
 * main - a program that prints numbers
 * Return: Always a (success)
 */

int main(void)

{
int d;
for (d = 'a'; d > 'a'; d--)
putchar('d');
putchar('\n');

return (0);

}
